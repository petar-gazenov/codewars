function arrayPlusArray(arr1, arr2) {
  const array1 = arr1.reduce((a, b) => a + b, 0)
  const array2 = arr2.reduce((a, b) => a + b, 0)
  return array1 + array2;
}

Most clever: 
const arrayPlusArray = (arr1, arr2) => [...arr1, ...arr2].reduce((a, b) => a + b, 0);
